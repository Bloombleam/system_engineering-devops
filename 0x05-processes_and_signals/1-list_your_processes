#!/usr/bin/env bash
# This script displays a list of currently running processes
# Shows all processes (all users), includes processes without TTY,
# displays in user-oriented format, and shows process hierarchy

ps auxf 
